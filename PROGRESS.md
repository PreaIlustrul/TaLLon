# PROGRESS.md — resume-here file

If you are a new session picking this up, read in order:
1. `memory/task_plan.md`  — what's done / what's next (checkboxes)
2. `memory/decisions.md`  — why things are the way they are
3. `memory/findings.md`   — research you don't need to redo
4. `memory/progress.md`   — detailed log of every session (newest at bottom)

Build:        `dotnet build src/TaLLon.sln -c Release`
Run daemon:   `dotnet run --project src/TaLLon.App`
Settings UI:  `dotnet run --project src/TaLLon.App -- --settings`
Shortcuts:    `powershell -ExecutionPolicy Bypass -File tools/install-shortcuts.ps1`

## Current status
See the checkboxes in `memory/task_plan.md`; the latest session log is at the bottom of
`memory/progress.md`.
